"""Drop-in replacements for vendored third-party components.

Kept here rather than as edits to ``vendor/`` so the vendored checkout stays a
clean, pinned copy of upstream that can be re-cloned or bumped at will.
"""
