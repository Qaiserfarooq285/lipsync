# Lip-sync stage (LatentSync) — isolated environment, Python 3.10.
#
# These are LatentSync's own pins (vendor/LatentSync @ a229c39) with two changes:
#   - gradio removed: we never launch the demo UI, and it drags in a large tree.
#   - mediapipe kept: needed by the commercially-clean face detector in
#     gpu/patches/mediapipe_face_detector.py (see docs/licenses.md).
#
# Python 3.10 is required because mediapipe==0.10.11 publishes no cp312 wheel.
# Provision it with:  uv python install 3.10  (scripts/setup_envs.sh does this)

torch==2.5.1
torchvision==0.20.1
diffusers==0.32.2
transformers==4.48.0
decord==0.6.0
accelerate==0.26.1
einops==0.7.0
omegaconf==2.3.0
opencv-python==4.9.0.80
mediapipe==0.10.11
python_speech_features==0.6
librosa==0.10.1
scenedetect==0.6.1
ffmpeg-python==0.2.0
imageio==2.31.1
imageio-ffmpeg==0.5.1
lpips==0.1.4
face-alignment==1.4.1
huggingface-hub==0.30.2
numpy==1.26.4
kornia==0.8.0
DeepCache==0.1.1

# Face detection. InsightFace's *weights* are non-commercial research only.
# Required by LatentSync's stock detector; opt in via video.face_detector=insightface.
# The default (mediapipe) does not load these.
insightface==0.7.3
onnxruntime-gpu==1.21.0
